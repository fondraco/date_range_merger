import 'package:date_range_merger/models/time_range_model.dart';

class TimeRangePickerHelper {


  /// Merges overlapping or consecutive time ranges
  /// The method sorts the time ranges by their start time and then merges them
  /// if the the ranges are overlapping on the 24 - 0 hour boundary, the method will merge them as well
  static List<TimeRange> mergeDateRanges(List<TimeRange> unmergedTimes) {
    List<TimeRange> mergedTimes = [];

    return unmergedTimes;
  }
}